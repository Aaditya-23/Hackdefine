import { Box, Typography } from "@mui/material";
import { container, text } from "./Styles";

export default function SocialMedia() {
  return (
    <Box sx={{ ...container }}>
      <Typography sx={{ ...text }} variant="h5" component="div">
        Follow Us on Instagram
          </Typography>

        
        
    </Box>
  );
}
